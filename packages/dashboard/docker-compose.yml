version: '3'
services:
  dashboard:
    image: transmute/sidetree-dashboard:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8080:8080'
  # mongo:
  #   image: mongo
  #   restart: always
  #   ports:
  #     - '27017:27017'
