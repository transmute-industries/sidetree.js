version: '3'
services:
  dashboard:
    image: transmute/sidetree-dashboard:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
  mongo:
    image: mongo
    restart: always
    ports:
      - '27017:27017'
