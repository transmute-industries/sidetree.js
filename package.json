{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "lint": "lerna run lint --stream",
    "test": "lerna run test --stream --concurrency 1 --since master",
    "build": "lerna run build --stream"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run lint --since HEAD"
    }
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "lerna": "^3.20.2"
  }
}
